// Placeholder to satisfy expo-router tab slot — handled via FloatingAddButton
import { Redirect } from 'expo-router';
export default function AddTab() {
  return <Redirect href="/transaction/add" />;
}
